name: Deploy-to-dev

on:
  push:
    branches:
      - dev
jobs:
  Build-and-Deploy:
    runs-on: 35.236.219.144
    environment: dev
    steps:
      - name: build
        run: npm install && npm run build v-y
      - name: deploy
        run: cd / && ls
     
